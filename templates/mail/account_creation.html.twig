    <p> Hello {{ user.getUsername() }}</p>
    
    <p>To confirm your email, please click the following link:</p>
    
    <a href="{{ absolute_url(path('user_activate', {'token': user.getEmailToken()})) }}">
    	{{ absolute_url(path('user_activate', {'token': user.getEmailToken()})) }}
    </a>

    